const BingoFactory = require('./bingo-factory');
const BingoPage = require('./bingo-page');
const config = require('../bingo.config');

module.exports = {
    BingoFactory,
    BingoPage,
    config
}; 