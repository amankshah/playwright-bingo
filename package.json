{
  "name": "playwright-bingo",
  "version": "1.0.0",
  "description": "A powerful Playwright framework with self-healing capabilities and page object model support",
  "main": "lib/index.js",
  "bin": {
    "bingo": "./bin/bingo.js"
  },
  "scripts": {
    "test": "cucumber-js",
    "test:allure": "rimraf allure-results && cucumber-js && allure generate ./allure-results --clean",
    "test:allure:open": "rimraf allure-results && cucumber-js && allure generate ./allure-results --clean && allure open allure-report"
  },
  "keywords": [
    "playwright",
    "cucumber",
    "bdd",
    "testing",
    "automation"
  ],
  "author": "",
  "license": "ISC",
  "files": [
    "bin",
    "lib",
    "templates"
  ],
  "dependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "@playwright/test": "^1.52.0",
    "allure-cucumberjs": "^3.2.1",
    "allure-playwright": "^3.2.1",
    "commander": "^11.1.0",
    "dotenv": "^16.5.0",
    "properties-reader": "^2.3.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
