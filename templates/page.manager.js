class PageManager {
    constructor(page) {
        this.page = page;
        this._initializePages();
    }

    _initializePages() {
        // Pages will be dynamically added here
    }
}

module.exports = PageManager; 